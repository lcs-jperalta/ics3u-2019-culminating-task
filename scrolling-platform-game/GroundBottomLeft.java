import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class GroundLeft here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class GroundBottomLeft extends Platform
{
    /**
     * Constructor
     * 
     * Called once when object is created.
     */
    GroundBottomLeft(int scrollableWorldX, int scrollableWorldY)
    {
        super(scrollableWorldX, scrollableWorldY);
    }
    
    /**
     * Act - do whatever the GroundBottomLeft wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {
        // Add your action code here.
    }    
}
